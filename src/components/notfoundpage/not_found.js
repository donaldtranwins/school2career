import React from 'react';

//creates the 404 page for pages that aren't found.
const NotFound = () =>
    (
    <div className="container col-sm-12">
        <img className="container col-sm-12" src="/images/404.png"/>
    </div>
    );

export default NotFound;